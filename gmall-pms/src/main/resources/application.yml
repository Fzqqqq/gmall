server:
  port: 18081
feign:
  sentinel:
    enabled: true
# 映射文件地址
mybatis-plus:
  mapper-locations: classpath:mapper/pms/*.xml
  # 别名扫描包
  type-aliases-package: com.atguigu.gmall.pms.entity
  # id的生成策略
  global-config:
    db-config:
      id-type: auto